package marketservice

import (
	"context"

	"gitlab.com/morbackend/mor_services/mpb"
)

func (svc *MarketService) AdminFreezeAccountTrade(ctx context.Context, req *mpb.ReqUserId) (*mpb.Empty, error) {
	return nil, nil
}
